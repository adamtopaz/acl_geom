import AclGeom.Geometry.Points

/-!
# The semantic relations Q, Q′, and J

`Q_sem`, `Q'_sem` on quadruples of points and the `j`-map with image `J_sem`
(blueprint Def semantic-configs), together with the normalization identities
(`[cx] = [x]`, `[x+c] = [x]`, Frobenius invariance). By the blueprint's
architecture rule, this file imports only closure/point foundations — no
geometric predicates.

**Status:** skeleton (M0); contents arrive with M4.

This module is part of the formalization of the Evans–Hrushovski–Gismatullin
reconstruction theorem; the source of truth is `sources/blueprint.tex`.
-/

namespace AclGeom

end AclGeom
