import AclGeom.Reconstruct.Base

/-!
# Scalar elimination and recovery of all points

The naturality scalar is one (blueprint Prop scalar-one), the two-generic
intersection lemma (Lemma two-generic-intersection), recovery of every point
including those inside `acl(a)` (Prop all-points), and the atomistic extension
to every closed subfield.

**Status:** skeleton (M0); contents arrive with M7 (checklist R2, R3, R4).

This module is part of the formalization of the Evans–Hrushovski–Gismatullin
reconstruction theorem; the source of truth is `sources/blueprint.tex`.
-/

namespace AclGeom

end AclGeom
