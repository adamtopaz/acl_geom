import AclGeom.Closure.Basic

/-!
# Finite one-variable correspondences

Loci and generic points (`I(a/k)`, `Loc_k a`, dimension = trdeg), finite
correspondences between curves via (8.1), composition through irreducible
components of fiber products, associativity, and the generic extension and
specialization lemma (blueprint §8.1, Lemma generic-extension).

**Status:** skeleton (M0); contents arrive with M3 (checklist C2).

This module is part of the formalization of the Evans–Hrushovski–Gismatullin
reconstruction theorem; the source of truth is `sources/blueprint.tex`.
-/

namespace AclGeom

end AclGeom
